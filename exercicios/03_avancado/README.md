# 🔴 Exercícios Avançados

Esta etapa é para quem já tem **boa base em Python**  
e quer começar a resolver **problemas mais próximos do mundo real**.

Aqui o foco não é só “fazer funcionar”, mas:
- pensar melhor a solução
- organizar o código
- entender por que algo funciona (ou não)

Não se cobre perfeição.  
Código bom nasce de código que já foi confuso um dia.

Vá com calma, teste bastante e aprenda no processo.

---

## 1️⃣ Sistema de cadastro com persistência

Crie um programa que:
- Cadastre usuários (nome, e-mail e idade)
- Salve os dados em um arquivo (JSON ou CSV)
- Permita listar, adicionar e remover usuários
- Carregue os dados automaticamente ao iniciar o programa

👉 Conceitos praticados:
arquivos, dicionários, listas, funções e organização de código.

---

## 2️⃣ Consumo de API pública

Crie um programa que:
- Consuma uma API pública (ex: clima, CEP, moedas)
- Trate erros de conexão ou dados inválidos
- Exiba as informações de forma organizada no terminal

👉 Conceitos praticados:
requisições HTTP, tratamento de erros e leitura de dados externos.

---

## 3️⃣ Sistema de login simples

Crie um sistema que:
- Cadastre usuários com login e senha
- Valide o acesso do usuário
- Bloqueie o acesso após algumas tentativas erradas
- Armazene os dados de forma segura (sem senha em texto puro)

👉 Conceitos praticados:
hash de senha, validação, controle de fluxo e segurança básica.

---

## 4️⃣ Automação de tarefas

Crie um script que:
- Leia arquivos de uma pasta
- Organize esses arquivos por tipo (PDF, JPG, TXT, etc.)
- Crie pastas automaticamente se não existirem
- Exiba um relatório do que foi feito

👉 Conceitos praticados:
`os`, `pathlib`, automação e manipulação de arquivos.

---

## 5️⃣ Mini projeto com arquitetura simples

Crie um pequeno projeto que:
- Tenha mais de um arquivo `.py`
- Separe responsabilidades (ex: lógica, interface, dados)
- Use funções ou classes de forma organizada
- Seja fácil de entender e manter

👉 Conceitos praticados:
organização de projeto, modularização e boas práticas.

---

💡 **Dica final**

Se parecer difícil, ótimo — você está evoluindo.  
O avanço começa exatamente onde o conforto termina.
